const frutas = ['Pera', 'Abacaxi', 'Uva'];

for(let i in frutas){
    console.log(frutas[i]);
}

const pessoa = {
    nome: 'Otavio',
    idade: 35
};

for (let j in pessoa){
     console.log(j, pessoa[j]);
}